class Solution {
public:
    string convert(string s, int numRows) {
        if(numRows == 1) return s;
        int n = s.length() , j=0;
        bool down = true;
        vector<string> words;
        for(int i=0 ; i<n ; i++){
            
        }
    }
};